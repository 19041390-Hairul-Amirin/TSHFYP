<div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">

        @if (User.IsInRole("manager") || (User.IsInRole("admin")))
        {
            <li class="nav-item">
                <a class="nav-link" asp-controller="Order" asp-action="SCM">Purchase Order</a>
            </li>
        }
        @if (User.IsInRole("manager") || (User.IsInRole("admin")))
        {
            <li class="nav-item">
                <a class="nav-link" asp-controller="Chart" asp-action="Name">DashBoard</a>
            </li>
        }
        @if (User.IsInRole("admin"))
        {
            <li class="nav-item">
                <a class="nav-link" asp-controller="Account" asp-action="Register">Register</a>
            </li>
        }
        @if (User.IsInRole("admin"))
        {
            <li class="nav-item">
                <a class="nav-link" asp-controller="Order" asp-action="Add">Add Purchase Order</a>
            </li>
        }
        <!--User List-->
        @if (User.IsInRole("admin"))
        {
            <li class="nav-item">
                <a class="nav-link" asp-controller="Order" asp-action="UserList">List of Users</a>
            </li>
        }
        <!--Supplier Value-->
        @if (User.IsInRole("manager"))
        {
            <li class="nav-item">
                <a class="nav-link" asp-controller="Chart" asp-action="Value">Dash</a>
            </li>
        }
    </ul>


   <ul class="nav navbar-nav ml-auto">
      <li>
         <p class="navbar-text">
            Welcome @User.Identity.Name<br />
         </p>
      </li>
      <li>
          <a class="nav-link" asp-controller="Order" asp-action="Logoff">
              <i class="fas fa-sign-out-alt"></i>
              Logout
          </a>
      </li>
      <li>
         &nbsp;&nbsp;&nbsp;
      </li>
   </ul>
</div>


